// ─── Module: atsPromptTemplates ───────────────────────────
// Description: Prompt templates for ATS analysis AI calls
// Coverage targets: getAtsSystemPrompt, getAtsUserPrompt, buildKeywordAnalysisPrompt
// Last updated: 2026-05-22

import { describe, it, expect, vi, beforeEach } from "vitest";

describe("atsPromptTemplates", () => {
  it("should load the system prompt from the file when available", () => {});
  it("should fall back to the hardcoded default when the file is missing", () => {});
  it("should interpolate resume content into the user prompt", () => {});
  it("should include target keywords in the keyword analysis prompt", () => {});
});
