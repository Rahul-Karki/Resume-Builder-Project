// ─── Module: google ───────────────────────────
// Description: Google OAuth token verification
// Coverage targets: verifyGoogleToken
// Last updated: 2026-05-22

import { describe, it, expect, vi, beforeEach } from "vitest";

describe("google", () => {
  describe("verifyGoogleToken", () => { it("should return the user payload when the Google token is valid", () => {}); it("should throw when the audience does not match the client ID", () => {}); it("should throw when the token is expired", () => {}); });
});
