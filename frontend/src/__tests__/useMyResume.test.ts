// ─── Module: useMyResume ───────────────────────────
// Description: Fetch user resumes and user profile
// Coverage targets: useMyResumes (resumes, user, loading, error, calculateCompletionScore, mapResumeDocumentToSavedResume, getResumePayload, isAuthError)
// Last updated: 2026-05-22

import { describe, it, expect, vi, beforeEach } from "vitest";

describe("useMyResume", () => {
  it("should fetch resumes on mount when the user is authenticated", () => {});
  it("should return an empty list when the user has no resumes", () => {});
  it("should calculate the completion score correctly", () => {});
  it("should map a resume document to a saved resume format", () => {});
  it("should detect auth errors from API responses", () => {});
});
